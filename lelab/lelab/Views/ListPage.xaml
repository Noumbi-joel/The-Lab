<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="LISTE"
             x:Class="lelab.Views.ListPage">
    <ContentPage.Content>
        <ListView x:Name="maListePage">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                            <Label Text="{Binding Nom}" VerticalOptions="Center" TextColor="Black" HorizontalOptions="FillAndExpand" Margin="15,0" />
                            <Label Text="{Binding Prix}" FontAttributes="Bold" TextColor="Black" VerticalOptions="Center" Margin="15,0" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>